from basic_scene import MyScene


class NerveOfALine(MyScene):
    def construct(self):
        self.create_title("Ejemplo del nervio de un intervalo")
        description = r"Un anillo es homotópicamente equivalente a un círculo. Y el círculo no es contráctil."